# 네이티브 갭 대장 — 웹 타깃이 재지 못한 것

- 작성: 2026-07-27
- 판단 근거: `k-journey/31-k-journey-decision-log-2026-07-25.md` **`DEC-029` 검증 방법 ④** · **`DEC-033`**
- 지적 출처: `k-journey/47-k-journey-role-review-unscored-dec-round2-2026-07-27.md` §2.1 **★9**
  (R05 Content Ops 「운영 대장 없으면 다음 학기에 바로 썩습니다」 · **기각 불가** · 등급 **A** ·
   R02 「푸시는 약속이 아닙니다」 · **기각 불가** · 등급 **A**)

---

## 0. 이 대장이 존재하는 이유

`DEC-029`는 관통 판정의 실행 타깃을 **웹**으로 고정하면서 **반례 ①**을 적었다 —
**「웹에서 관통이 성립해도 iOS·Android에서 성립한다는 증거가 아니다.」**
그리고 그 반례의 **계측 수단으로 이 파일을 지정**하고, 스스로 이렇게 적었다:

> **「이 대장이 비어 있으면 반례 ①을 「없다」가 아니라 「재지 않았다」로 읽는다.」**

**2026-07-27 채점 시점에 이 파일은 비어 있는 것이 아니라 없었다.**
그래서 반례 ①의 상태는 **「재지 않았다」**가 맞았다. 이 대장이 그것을 **「쟀고, 이만큼 비어 있다」**로 바꾼다.

⛔ **이 대장은 갭을 없애지 않는다. 갭의 크기를 적을 뿐이다.**
여기 적힌 `REQ`는 **이번 구현으로 검증되지 않았다** — `k-journey/46-k-journey-implementation-result-2026-07-27.md`와
`../06-portfolio-project-lineup-2026-07-24.md` **§4.1**의 「쓰면 안 되는 문장」이 이 목록에 걸린다.

---

## 1. 모듈별 갭 — 4종

측정: 2026-07-27 · `grep -rl "<모듈>" app/ src/ | wc -l`

| # | 모듈 | 참조 파일 | 웹에서 무엇이 안 되는가 | **안 덮이는 `REQ` / `POL`** | 이번 구현의 판정 |
|---|---|---|---|---|---|
| 1 | **`expo-notifications`** | **2** — `src/lib/notifications.ts` · `src/lib/__tests__/notifications.test.ts` | **푸시·로컬 알림 전달이 no-op이다.** 웹 빌드에서 권한 요청·스케줄링이 실제 OS 알림으로 이어지지 않는다 | **`REQ-INR-002`**(알림 **3중** — 푸시 · 로컬 알림 · 홈 기한 표시. **앞의 2중이 웹에서 재현되지 않는다**) · **`REQ-COR-002`**(푸시를 필수 조건으로 만들지 않는다 — **「푸시가 실패해도」의 앞쪽 절반을 만들 수 없다**) · `POL-009` · 축 2 전이 **`E7`**(`permission_denied`) | ⛔ **미검증.** 3중 중 **홈 기한 표시 1중만** 관통 판정이 확인했다(`D-78` · Oct 13 · ON TRACK) |
| 2 | **`expo-media-library`** | **1** — `src/lib/share.ts` | **기기 앨범·파일로 내보내기가 웹에 없다.** 브라우저 다운로드로 대체되며 **동작이 다르다** | **`REQ-SFR-012`**(데이터 내보내기 — 조건 축·태스크 상태를 텍스트로 추출. **`Should`**) · `POL-010` | ⛔ **미검증.** `Should` 우선순위라 P0 슬라이스 범위 밖이기도 하다 — **두 이유가 겹쳐 있다는 것을 적어 둔다** |
| 3 | **`crashlytics`** (`@react-native-firebase/crashlytics`) | **3** — `app/_layout.tsx` · `src/lib/errorAlert.ts` · `src/lib/notifications.ts` | **크래시 수집이 웹에서 동작하지 않는다.** 네이티브 크래시 개념 자체가 없다 | `POL-012` **고지 대조표 「크래시 로그」 행**(현재 **`미확인` — SDK 미선정**) · `REQ-PER-002` 계열의 실패 관측 | ⛔ **미검증.** **다만 이 행은 원래 `미확인`이었다** — 웹 때문에 비어진 것이 아니라 **처음부터 비어 있었다.** 두 공백을 구분해 적는다 |
| 4 | **`posthog`** | **27** | **웹에서 SDK는 로드되나 키가 없어 `disabled`다.** 웹 자체의 제약이 아니라 **환경 변수 미주입**이다 — **네이티브에서도 같다** | `REQ-SER-003` · `REQ-SER-005` · `POL-001` **해상도 규칙 1·2·3** · `POL-012` **고지 대조** · `DEC-027` 검증 ①·② · `DEC-030` 검증 ③ · `DEC-025` 검증 방법 | ⛔ **미검증.** ⚠️ **이 한 줄만 성격이 다르다 — 네이티브 갭이 아니라 설정 갭이다.** iOS·Android로 바꿔도 **키가 없으면 똑같이 안 흐른다** |

```bash
# 재현 — 모듈별 참조 파일 수 (2026-07-27 측정값: 2 / 1 / 3 / 27)
cd /Users/vozmelodiosa/Desktop/k-journey-v2
for m in expo-notifications expo-media-library crashlytics posthog; do
  printf "%s: %s\n" "$m" "$(grep -rl "$m" app/ src/ 2>/dev/null | wc -l)"; done
# posthog 가 값을 흘리는가 — intakeSeason 0건이면 페이로드가 비어 있다
grep -rho 'intakeSeason' app/ src/ | wc -l    # 0
```

---

## 2. 갭이 두 종류다 — 섞지 않는다

| 종류 | 무엇 | 해당 | 어떻게 닫히는가 |
|---|---|---|---|
| **A. 타깃 갭** | **웹이라서** 재지 못한다. 네이티브 런타임이 있어야 판정된다 | **1 · 2 · 3** | `DEC-029` 되돌릴 조건 ② — **Xcode가 설치되면** iOS로 재판정한다. **그때 웹 결과를 버리지 않고 둘 다 적는다** |
| **B. 설정 갭** | **타깃과 무관**하다. 키·환경 변수가 없어 안 흐른다 | **4** | `DEC-029` 되돌릴 조건 ③ — **PostHog 키를 주입하면** 웹에서도 그 자리에서 닫힌다 |

> ⚠️ **이 구분이 이 대장의 핵심이다.** 넷을 한 덩어리로 적으면 **「네이티브로 가면 다 풀린다」로 읽히는데 4번은 그렇지 않다.**
> 반대로 4번을 근거로 「웹 타깃이 문제였다」고 말하는 것도 틀렸다 — **4번은 웹 전환 이전부터 있던 상태다.**

---

## 3. 이 대장이 `06` 포트폴리오 표기에 미치는 것

`../06-portfolio-project-lineup-2026-07-24.md` **§4.1**의 「쓸 수 있는 / 쓰면 안 되는 문장」에 걸린다.

| 쓸 수 있다 | 쓰면 안 된다 |
|---|---|
| 「**웹 타깃에서** 조건 축 입력 → 홈 → 태스크 상세 → 완료가 **관통했다**」 | 「앱이 돈다」 · 「알림이 동작한다」 · 「분석 이벤트가 수집된다」 |
| 「기한 표시는 **홈에서** 확인했다」 | 「알림 3중이 동작한다」 — **3중 중 1중만 봤다** |
| 「분석 규칙을 **명세로 정했다**」 | 「분석 규칙을 **검증했다**」 — 페이로드가 없다 |

---

## 4. 이 대장이 하지 않는 것

- **갭을 메우지 않는다.** 네 항목 전부 **미검증으로 남는다.**
- **웹 타깃 결정을 되돌리지 않는다.** `DEC-029`는 2026-07-27에 **확정**됐다(`47` §2.1 · `V5` 충족).
- **`46` §6.1의 남은 코드 결함 4건은 여기 없다** — 그것은 웹 갭이 아니라 **구현 결함**이고 별도 목록이다.
- **미해소 ⓕ**(웹에서 푸시 fallback을 재현할 수 없다)는 **1번 행이 그 답이다** — 어느 `REQ`가 안 덮이는지를 적은 것이 이 대장의 몫이었다.

---

## 5. 갱신 규칙

**새 네이티브 모듈을 추가하면 이 표에 행을 먼저 추가한다.**
행을 추가할 수 없으면 — 즉 **웹에서 무엇이 안 되는지 말할 수 없으면** — 그 모듈을 추가하지 않는다.

**최종 확인처**: 네이티브 런타임(iOS 시뮬레이터 또는 실기기)에서의 재판정.
`DEC-029` 되돌릴 조건 ②가 그 시점을 정한다.
